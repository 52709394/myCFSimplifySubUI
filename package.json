{
  "name": "my-cf-simplify-sub-ui",
  "version": "52709394",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --env dev",
    "start": "wrangler dev --env dev",
    "test": "vitest"
  },
  "devDependencies": {
    "wrangler": "^4.18.0"
  },
  "dependencies": {
    "@noble/curves": "^2.2.0",
    "js-yaml": "^4.1.1",
    "qr-image": "^3.2.0"
  }
}
